---
title: "Events and payloads"
sidebarTitle: "Events and payloads"
description: "The four public milestones, their exact fields, and when subscribers receive them."
---

Subscribe with `window.FittingMe.on(handler)`. Each event identifies its embed with `encart`, the container DOM element. Event names and some values are French identifiers; keep their spelling and accents in code.

## Event catalogue

| `jalon` | Additional fields | Meaning | Replayed to a new subscriber? |
| --- | --- | --- | --- |
| `amorçage` | `issue`; optional `code_transport` | Latest bootstrap outcome. | Yes, for each live embed with a known outcome. |
| `ouverture` | `origine`: `tap` or `commande` | Drawer opening was announced and accepted by the loader. | No. |
| `fermeture` | None | A previously announced open drawer closed. | No. |
| `complétion` | None | A completed result was observed when the drawer closed; sent after `fermeture`. | No. |

Every event also has `jalon` and `encart`. There is no timestamp, product identifier, session identifier, shopper photo, result URL, or order identifier in this public payload.

## Bootstrap outcome

| `issue` | Interpretation |
| --- | --- |
| `éligible` | The embed can offer its journey. You may enable a custom trigger. |
| `inéligible` | This embed has no journey to open for the current product context. |
| `indéterminé` | Bootstrap did not establish eligibility. Treat this as a failure to determine availability, not an ineligible product. |

Eligibility is not a successful session or generation. Key-domain permission is checked when a session opens, and a supplied garment image is downloaded and decoded later.

`code_transport` may be a string on an indeterminate event. Use it as diagnostic information; do not infer a business reason from an HTTP transport outcome.

## Opening origin

- `tap`: the shopper used the widget-provided entry point.
- `commande`: the host page requested opening through the loader.

A custom button that calls `open(container)` produces `commande`, even though a shopper clicked that button.

## Timing and ownership

Register tracking before enabling the shopper to interact. A late subscription receives bootstrap state but cannot recover previous opens or completions. Bootstrap replay must not be counted as a new shopper visit.

Each subscription sees all embeds. Filter using DOM element identity and attach your own product context from the component or page that owns that embed. Remove the subscription when its owner is disposed.

A close event follows an opening that was actually announced. Calling `close()` on an already-closed embed need not produce an event. Likewise, an accepted command is not itself an analytics event.

## Completion is not conversion

`complétion` is emitted **on drawer closure, after `fermeture`**, when the journey has a completed result that this embed has not already announced. Displaying the result alone emits no completion. Closing and reopening the same completed result in the same embed does not repeat it; a later incomplete journey can rearm the event.

A fresh embed or page reload can resume that same result and emit completion again when closed. This is a new emission, despite the event not being replayed to late subscribers. There is no public session or event ID to deduplicate it across embeds or reloads.

Destroying an embed or unsubscribing inside `fermeture` can drop the following `complétion`. The public loader API has no notification that all close-related messages have been delivered. Keep the embed and tracking subscription alive after closure when observing completion. Do not use arbitrary timers as a delivery guarantee.

Treat this as an observation of a completed result at close, not a count of unique generations. It does not establish that the shopper bought the item, selected a size, or added anything to a cart. Do not fabricate an image-download or order payload from this event.

For a complete tracking adapter, see [track engagement and try-on results](/integration/analytics).
